{
    "name": "@smart-invoice-analyzer/infra",
    "version": "1.0.0",
    "private": true,
    "main": "bin/app.js",
    "scripts": {
        "build": "echo 'No build for infra'",
        "watch": "tsc -w",
        "cdk": "cdk",
        "deploy:dev": "cdk deploy sia-dev --require-approval never",
        "deploy:pipeline": "cdk deploy PipelineStack-Staging PipelineStack-Prod --require-approval never",
        "synth": "cdk synth",
        "lint": "echo 'No lint for infra'",
        "check-types": "tsc --noEmit"
    },
    "devDependencies": {
        "@types/jest": "^30",
        "@types/node": "^24.10.1",
        "aws-cdk": "2.1108.0",
        "jest": "^30",
        "ts-jest": "^29",
        "ts-node": "^10.9.2",
        "typescript": "~5.9.3"
    },
    "dependencies": {
        "aws-cdk-lib": "^2.240.0",
        "constructs": "^10.5.0"
    }
}
