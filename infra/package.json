{
    "name": "@vault/infra",
    "version": "0.1.0",
    "private": true,
    "main": "bin/app.js",
    "scripts": {
        "build": "echo 'No build for infra'",
        "watch": "tsc -w",
        "lint": "echo 'No lint for infra'",
        "typecheck": "tsc --noEmit",
        "dev:synth": "cdk synth Vault-Dev",
        "dev:diff": "cdk diff Vault-Dev",
        "dev:deploy": "cdk deploy Vault-Dev --require-approval never",
        "prod:synth": "cdk synth Vault-Prod",
        "prod:diff": "cdk diff Vault-Prod",
        "prod:deploy": "cdk deploy Vault-Prod --require-approval never"
    },
    "devDependencies": {
        "@types/jest": "^30",
        "@types/node": "^24.10.1",
        "aws-cdk": "2.1108.0",
        "jest": "^30",
        "ts-jest": "^29",
        "ts-node": "^10.9.2",
        "typescript": "^6.0.3"
    },
    "dependencies": {
        "aws-cdk-lib": "^2.240.0",
        "constructs": "^10.5.0"
    }
}
